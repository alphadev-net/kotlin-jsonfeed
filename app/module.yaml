product: jvm/app

dependencies:
  - ../lib
  - $libs.ajalt.clikt
  - $libs.ktor.client

settings:
  publishing:
    enabled: false
    name: jsonfeed-demo
  kotlin:
    optIns:
      - kotlin.time.ExperimentalTime

apply:
  - ../publish.module-template.yaml
