product: jvm/app

dependencies:
  - ../lib
  - $libs.ajalt.clikt
  - $ktor.client.cio

settings:
  publishing:
    enabled: false
    name: jsonfeed-demo
  kotlin:
    optIns:
      - kotlin.time.ExperimentalTime
  ktor: enabled

apply:
  - ../publish.module-template.yaml
